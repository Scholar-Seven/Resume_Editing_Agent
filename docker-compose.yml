version: "3.9"
services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DEEPSEEK_API_KEY =【add_your_deepseek_api_key_here】
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
